# Figure Notes - LinkedIn post 7460093976893755392

## Figure 01 - LinkedIn feedshare / X screenshot

- Status: recovered
- File: `figures/feedshare-image.jpg`
- Source: LinkedIn JSON-LD `image.url`; volatile media query tokens are redacted in stored metadata.
- What it shows: A screenshot of an X/Twitter post by Tibo (`@thsottiaux`) quote-posting Daniel San (`@dani_avila7`). The visible text says Claude Code 2.1.139 added `/goal`; setting a completion condition lets Claude continue across turns until the condition is satisfied; it works in interactive mode, `-p`, and Remote Control. The embedded terminal screenshot shows `Claude Code v2.1.139` and a slash-command hint: `/goal [<condition> | clear]`.
- Why it matters: The image is the visual trigger for the LinkedIn post's opening claim that Claude Code now has a Codex-like `/goal` feature. It also reinforces the post's main frame: autonomous agents need explicit completion conditions, not just prompts.
- Evidence status: Secondary/social screenshot. It is useful context but not the primary verification source. The official Claude Code Markdown snapshot in `source/claude-code-goal.md` is stronger evidence for the existence and semantics of Claude `/goal`.
- Limitations: The screenshot includes social engagement counts and translated text that are volatile. It does not independently prove release chronology, feature availability in every environment, or exact version rollout.
